server:
  port: 8990
spring:
  application:
    name: CONFIGCLIENT
  cloud:
    consul:
      # host: localhost
      # port: 8500
      discovery:
        service-name: ${spring.application.name}
  # 配置指定环境
  profiles:
    active: dev
